# 📊 Схема базы данных

## Общая информация

- **Тип БД:** SQLite 3
- **Файл:** `data/bot_database.db`
- **Кодировка:** UTF-8

## Таблицы

### 👥 users (пользователи)

Хранит информацию о всех пользователях бота.

| Поле | Тип | Описание |
|------|-----|----------|
| `user_id` | INTEGER PRIMARY KEY | ID пользователя ВК |
| `first_name` | TEXT | Имя |
| `last_name` | TEXT | Фамилия |
| `role` | TEXT DEFAULT 'user' | Роль (owner/management/senior/junior/user) |
| `registered_at` | TIMESTAMP | Дата регистрации |
| `last_activity` | TIMESTAMP | Последняя активность |

**Пример:**
```sql
INSERT INTO users (user_id, first_name, last_name, role) 
VALUES (123456, 'Иван', 'Иванов', 'owner');
